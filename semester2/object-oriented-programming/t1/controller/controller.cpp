#include "controller.h"

/*
bool Controller::addTask(Task task) {
    if(this->repository.getIndex(task) != -1)
        return false;
    else {
        this->repository.add(task);
        return true;
    }
}*/